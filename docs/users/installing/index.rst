.. _file_install:

Installation
############

TTiP requires Python3 and Firedrake in order to run.
The easiest and fastest way to access Firedrake is by using the docker install.
If docker is not an option then Firedrake can be installed without it but this
can take several hours.

.. toctree::
   :maxdepth: 2
   :titlesonly:
   :caption: Contents:

      Firedrake - Docker (*recommended*) <docker>
      Firedrake - Manual <firedrake>
      TTiP <ttip>
